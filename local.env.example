# Copy to local.env and set your Graylog UDP GELF input. local.env is gitignored.
#   set -a && . ./local.env && set +a
#   rtl_433 -F json | ./rtl_433-graylog.py
GRAYLOG_HOST="graylog"
GRAYLOG_PORT="12201"
